#include "text_actions.h"

/* void type_text(*char text) {
    for (int i = 0; text[i] != '\0'; i++) {
        addch(text[i]); 
        refresh(); 
        napms(30); 
   } 
} */

/* int main(){
    char* text1 = ["This is a test case"];
    type_text(text1);
} */
